# The Charity Canvas — static preview

A frozen, read-only preview of the canvas. The editor is the real one: draw, zoom,
import an image and trace it. Buying is switched off, because GitHub Pages serves
files and cannot run the server the real site needs.

Rebuild from the app repo:

    npm run build:static -- ../../oskar14156.github.io/canvas /canvas/
